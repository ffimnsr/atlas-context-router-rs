[package]
name = "atlas-core"
version = "1.1.1"
edition = "2024"
description = "Core domain models, kinds, and error types shared across Atlas crates"
publish = false

[dependencies]
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
camino.workspace = true
time = { workspace = true, features = ["formatting"] }

[lints]
workspace = true
