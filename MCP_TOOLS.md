# MCP Tools

This file is generated from rmcp-backed `atlas_mcp::tool_list()` descriptors serialized from `rmcp::model::Tool`. Do not edit by hand.

MCP quick guidance:
- protocol versions: `2026-07-28, 2025-11-25`
- call `server/discover` for capability negotiation
- on stdio and HTTP requests after discovery, include `params._meta` with protocol version and client capabilities
- use explicit `arguments.repo_root` or server launch cwd for repo selection; do not rely on MCP Roots
- HTTP transport is stateless at protocol level: no `Mcp-Session-Id`, `GET /mcp`, `DELETE /mcp`, or `Last-Event-ID` flow

Result contract legend:
- `stable-object`: JSON `structuredContent` is source of truth; `outputSchema` validates that object.
- `text-only`: consume MCP `content`; no `outputSchema` advertised.

| Tool | Title | Input schema | Result contract | Output schema | Description |
|------|-------|--------------|-----------------|---------------|-------------|
| `analyze_architecture` | Analyze Architecture | object; required fields: 0 | `stable-object` | exact structuredContent schema | Analyze module-level cycles, layer violations, and coupling hotspots. JSON output matches the CLI insights architecture report; JSON output stays compact unless verbose=true. |
| `analyze_dead_code` | Analyze Dead Code | object; required fields: 0 | `stable-object` | exact structuredContent schema | Detect dead-code candidates: private/unexported code symbols (functions, methods, structs/types, traits, enums, interfaces, constants, variables) with no inbound semantic edges, not in the entrypoint allowlist, and not tests. Returns candidates with certainty tiers and blocker flags. Defaults to code symbols only. |
| `analyze_dependency` | Analyze Dependency | object; required fields: 1 | `stable-object` | exact structuredContent schema | Check whether removing a symbol is safe by verifying it has no remaining semantic references. Returns removable verdict, blocking callers, confidence tier, and suggested cleanups. |
| `analyze_metrics` | Analyze Metrics | object; required fields: 0 | `stable-object` | exact structuredContent schema | Analyze graph health metrics, outliers, complexity hotspots, and coupling findings. JSON output matches the CLI insights metrics report; JSON output stays compact unless verbose=true. |
| `analyze_patterns` | Analyze Patterns | object; required fields: 0 | `stable-object` | exact structuredContent schema | Detect repeated call chains, isolated structures, hubs, bottlenecks, and deep dependency paths. JSON output matches the CLI insights patterns report; JSON output stays compact unless verbose=true. |
| `analyze_remove` | Analyze Remove | object; required fields: 1 | `stable-object` | exact structuredContent schema | Analyse the impact of removing one or more symbols. Returns impacted symbols, files, and tests separated by confidence tier (Definite/Probable/Weak), plus uncertainty flags. |
| `analyze_safety` | Analyze Safety | object; required fields: 1 | `stable-object` | exact structuredContent schema | Score how safe it is to refactor a symbol. Returns fan-in, fan-out, test adjacency, cross-module caller count, and a 0–1 safety score with band (safe/moderate/risky) and suggested validations. |
| `assess_risk` | Assess Risk | object; required fields: 1 | `stable-object` | exact structuredContent schema | Score deterministic risk for one symbol with factor evidence and ranked findings. JSON output matches the CLI insights risk report; JSON output stays compact unless verbose=true. |
| `batch_query_graph` | Batch Query Graph | object; required fields: 1 | `stable-object` | exact structuredContent schema | Run multiple query_graph searches in a single round-trip. Canonical input is top-level `items`: an explicit array of query_graph-shaped objects. Returns stable object `structuredContent` with normalized `items`, per-item `results`, `summary`, and `warnings`. Each item uses same symbol-name FTS as query_graph — pass short exact identifiers, not natural-language phrases. File nodes remain opt-in per item via include_files=true. Max 20 items per call. |
| `broker_status` | Broker Status | object; required fields: 0 | `stable-object` | exact structuredContent schema | Return a lightweight health/ready check for the MCP broker process itself. Reports process uptime, PID, server version, and configured worker threads. Does NOT check graph readiness — use `status` or `doctor` for graph health. Useful for liveness probes and connectivity verification independent of graph state. |
| `build_or_update_graph` | Build Or Update Graph | object; required fields: 0 | `stable-object` | exact structuredContent schema | Scan, parse, and persist the code graph. Canonical input is `operation={ kind: 'build' }` for full scan or `operation={ kind: 'update', change_source: ... }` for incremental update. |
| `compact_session` | Compact Session | object; required fields: 0 | `stable-object` | exact structuredContent schema | Compact and curate the session event ledger. Removes stale low-value events, merges repeated actions, deduplicates reasoning outputs, and promotes high-value events to survive future eviction. Returns curation stats. Safe to call repeatedly. |
| `concept_clusters` | Concept Clusters | object; required fields: 1 | `stable-object` | exact structuredContent schema | Cluster files related to the given seed files by shared symbol references. Returns groups of co-dependent files ordered by coupling density. |
| `cross_file_links` | Cross File Links | object; required fields: 1 | `stable-object` | exact structuredContent schema | Find files that reference symbols defined in the given file. Returns semantic links ordered by coupling strength (number of shared symbol references). |
| `cross_session_search` | Cross Session Search | object; required fields: 1 | `stable-object` | exact structuredContent schema | CM11: Search saved context artifacts across all sessions for this repo. Returns stable object `structuredContent` with `query`, distinct `sessions`, preview `matches`, `summary`, `truncated`, and `warnings`. Use this for cross-session recall when the relevant content may have been saved in a prior session. |
| `db_check` | DB Check | object; required fields: 0 | `stable-object` | exact structuredContent schema | Run SQLite integrity check, scan for orphan nodes (no edges) and dangling edges (missing endpoint), and validate the session-side memory schema. Returns ok=true when all checks pass. Use to diagnose corrupt or inconsistent graph rows or a missing memories table. |
| `debug_graph` | Debug Graph | object; required fields: 0 | `stable-object` | exact structuredContent schema | Return internal graph diagnostics: node/edge counts by kind, top files by node count, orphan nodes, and dangling edges. Use to investigate structural anomalies or unexpected empty results. |
| `detect_changes` | Detect Changes | object; required fields: 1 | `stable-object` | exact structuredContent schema | List files changed since a base git ref, with per-file node counts from the graph. Use `change_source={ kind: ... }` with `base`, `staged`, or `working_tree`. |
| `doctor` | Doctor | object; required fields: 0 | `stable-object` | exact structuredContent schema | Run full repo health checks: git root detection, .atlas dir, config file, DB open/integrity, graph build state, tracked file count, and retrieval-index state. Returns an array of per-check results with pass/fail and detail. Call before trusting graph-backed context after a fresh clone or suspected corruption. |
| `explain_change` | Explain Change | object; required fields: 1 | `stable-object` | exact structuredContent schema | Advanced impact analysis for a set of changed files: risk level, changed-symbol breakdown by change kind (api/signature/internal), boundary violations, test coverage gaps, and a compact summary. Deterministic, LLM-free. |
| `explain_query` | Explain Query | object; required fields: 0 | `stable-object` | exact structuredContent schema | Explain how a query_graph invocation would be executed: tokenisation, FTS phrase construction, regex validation, and expected search path. Use to diagnose why query_graph returns no results or to validate a regex pattern before running it. |
| `find_complex_functions` | Find Complex Functions | object; required fields: 0 | `stable-object` | exact structuredContent schema | Find complex functions repo-wide or within selected files using deterministic complexity thresholds. JSON output matches the CLI insights complex-functions report; JSON output stays compact unless verbose=true. |
| `find_duplicates` | Find Duplicates | object; required fields: 0 | `stable-object` | exact structuredContent schema | Find exact-normalized and near-duplicate callable bodies using normalized token shingles. JSON output matches the CLI duplicates report; JSON output stays compact unless verbose=true. |
| `find_large_functions` | Find Large Functions | object; required fields: 0 | `stable-object` | exact structuredContent schema | Find large or complex functions repo-wide or within selected files using deterministic LOC and complexity thresholds. JSON output matches the CLI insights report; JSON output stays compact for agent review. |
| `find_similar_functions` | Find Similar Functions | object; required fields: 1 | `stable-object` | exact structuredContent schema | Find semantically similar functions for one callable symbol using name, signature, body-shingle, and neighbor overlap. JSON output matches the CLI similar-functions report; JSON output stays compact unless verbose=true. |
| `get_context` | Get Context | object; required fields: 1 | `stable-object` | exact structuredContent schema | Build bounded context around a symbol, file, or change-set. Canonical input is `target={ kind: 'query'\|'file'\|'files', ... }`. Supported query grammar: plain identifier (`compute`), exact qualified name (`src/service.rs::fn::compute`), `who calls <symbol>`, `what breaks <symbol>`, and `tests for <symbol>`. Natural-language-only descriptions without a concrete identifier are rejected with retry guidance. When changed files include docs, config, templates, SQL, or prompts, pass those paths in files target to merge graph and content assets under one bounded selection, ranking, and truncation policy. |
| `get_context_stats` | Get Context Stats | object; required fields: 0 | `stable-object` | exact structuredContent schema | Return storage statistics for the current (or specified) session: event count, saved source count, chunk count, and DB paths. |
| `get_docs_section` | Get Docs Section | object; required fields: 2 | `stable-object` | exact structuredContent schema | Resolve a Markdown section from a repo-relative documentation file using `selector={ kind: 'heading'\|'line', ... }`. Returns the section excerpt with heading metadata and file hash. |
| `get_global_memory` | Get Global Memory | object; required fields: 0 | `stable-object` | exact structuredContent schema | CM11: Return the cross-session global memory summary for this repo: frequently-accessed symbols and files, and recurring workflow patterns. Optionally provide focus_symbols and focus_files to also find past sessions most relevant to the current work context. |
| `get_impact_radius` | Get Impact Radius | object; required fields: 1 | `stable-object` | exact structuredContent schema | Compute nodes and files affected when the given files change. Returns compact, capped results. Change-source conflicts return structured retry guidance instead of a bare ambiguity error. |
| `get_minimal_context` | Get Minimal Context | object; required fields: 1 | `stable-object` | exact structuredContent schema | Auto-detect changed files from git, then return a compact review bundle: changed symbols, immediate impact, risk flags. Lower token overhead than get_review_context. |
| `get_review_context` | Get Review Context | object; required fields: 1 | `stable-object` | exact structuredContent schema | Assemble review context for the given files: changed symbols, impacted neighbors, critical edges, and risk summary. Agent-optimized output. Change-source conflicts return structured retry guidance instead of a bare ambiguity error. |
| `get_session_status` | Get Session Status | object; required fields: 0 | `stable-object` | exact structuredContent schema | Return the status of the current MCP session: identity, event count, last compaction time, and whether a resume snapshot exists. |
| `infer_modules` | Infer Modules | object; required fields: 0 | `stable-object` | exact structuredContent schema | Infer module buckets from package ownership, path layout, and dependency structure. JSON output matches the CLI infer-modules report; JSON output stays compact unless verbose=true. |
| `label_components` | Label Components | object; required fields: 0 | `stable-object` | exact structuredContent schema | Label files and symbols with Atlas component taxonomy such as cli, mcp, parse, review_context, and session_continuity. JSON output matches the CLI label-components report; JSON output stays compact unless verbose=true. |
| `list_graph_stats` | List Graph Stats | object; required fields: 0 | `stable-object` | exact structuredContent schema | Return node/edge counts and language breakdown for the indexed graph. |
| `man` | Man | object; required fields: 2 | `stable-object` | exact structuredContent schema | Return authoritative runtime manual documentation for one visible exported MCP tool without executing that target tool. Requires namespace='mcp' and exact case-sensitive tool_name lookup from the live registry. |
| `memory_recall` | Memory Recall | object; required fields: 1 | `stable-object` | exact structuredContent schema | Recall memories for this repo with the same lexical ranking, defaults, and visibility rules as `atlas memory recall`. Exact topic matches rank first; the viewer is the derived mcp session. Use shared=true for project + global memories only. |
| `memory_store` | Memory Store | object; required fields: 1 | `stable-object` | exact structuredContent schema | Store a memory record for this repo through the shared memory service (same fields, defaults, and validation as `atlas memory store`). Returns the persisted memory record including its id, scope, importance, and source_id. |
| `postprocess_graph` | Postprocess Graph | object; required fields: 0 | `stable-object` | exact structuredContent schema | Run explicit derived-analytics postprocessing after build/update without reparsing source files. Supports full or changed-only mode, optional single-stage execution, and dry-run lifecycle preview. |
| `purge_saved_context` | Purge Saved Context | object; required fields: 0 | `stable-object` | exact structuredContent schema | Delete saved artifacts. Provide session_id to delete all artifacts for that session, or omit to apply age-based cleanup (default: keep last 30 days). |
| `query_graph` | Query Graph | object; required fields: 0 | `stable-object` | exact structuredContent schema | Full-text search the code graph by symbol name, qualified name, or supported intent phrase. Supported `text` grammar: plain identifier (`compute`), exact qualified name (`src/service.rs::fn::compute`), `who calls <symbol>`, `what breaks <symbol>`, and `tests for <symbol>`. Returns stable object `structuredContent` with `query`, `matches`, `summary`, `truncated`, and `warnings`; set include_files=true when file-level hits are also useful. It does not return caller/callee usage edges. Empty `regex` is treated like omitted; truly empty `text`+`regex` requests return a self-correcting retry example instead of a bare validation failure. Natural-language-only descriptions without a concrete identifier are rejected with retry guidance. Follow up with symbol_neighbors, traverse_graph, or get_context when you need relationships. |
| `read_file_around_match` | Read File Around Match | object; required fields: 2 | `stable-object` | exact structuredContent schema | Read grouped snippets around literal or regex matches inside one repo-relative file. Use this when the file path is known and you need nearby context around matched lines. |
| `read_file_excerpt` | Read File Excerpt | object; required fields: 2 | `stable-object` | exact structuredContent schema | Read bounded file content from a repo-relative path using `selector={ kind: 'range'\|'ranges'\|'context', ... }`. Use this when you already know the file path and need precise excerpts instead of content search. |
| `read_saved_context` | Read Saved Context | object; required fields: 1 | `stable-object` | exact structuredContent schema | Retrieve the full content of a saved artifact by source_id. Supports paging via chunk_offset and max_bytes for large artifacts. Enforces session and repository scoping so cross-session reads are blocked. |
| `record_session_event` | Record Session Event | object; required fields: 1 | `stable-object` | exact structuredContent schema | Session capture for hosts without native LLM hooks, mirroring `atlas hook <event>` exactly. Records the same agent events through the shared event service: session-start, user-prompt, pre-tool-use, post-tool-use, pre-compact, post-compact, stop, session-end, file-changed, tool-failure, error, and the other supported hook events. Returns stored event identity, storage routing, resume snapshot state, and executed actions (lifecycle, prompt routing, graph refresh, freshness, review refresh). |
| `repo_registry` | Repo Registry | object; required fields: 0 | `stable-object` | exact structuredContent schema | Inspect persisted multi-repo registry metadata under .atlas/, including registered repo identities, relationship kinds, trust states, and bootstrap warnings. |
| `resolve_symbol` | Resolve Symbol | object; required fields: 1 | `stable-object` | exact structuredContent schema | Resolve a symbol name or exact qualified_name to its canonical graph symbol. Useful before relationship tools when you want one exact target from either a plain identifier (`compute`) or a fully qualified name (`src/service.rs::fn::compute`). Eliminates the manual workflow of query_graph → copy qualified_name → call symbol_neighbors. Returns the best match, an ambiguity list when multiple symbols match, and follow-up suggestions. Accepts public kind aliases (e.g. 'function'/'fn', 'struct'/'record') that are mapped to the compact tokens used in qualified names. |
| `resume_session` | Resume Session | object; required fields: 0 | `stable-object` | exact structuredContent schema | Retrieve and optionally consume the resume snapshot for the current (or specified) session. Builds a snapshot on demand if one does not exist. |
| `save_context_artifact` | Save Context Artifact | object; required fields: 2 | `stable-object` | exact structuredContent schema | Index and store a large tool output or context payload. Returns a pointer (source_id) for large content, a preview for medium content, or the raw string for small content. |
| `search_content` | Search Content | object; required fields: 1 | `stable-object` | exact structuredContent schema | Search file contents by literal string or regex. Use as a graph/content companion lookup when changed symbols depend on non-code text — config keys, SQL queries, prompt content, error messages, comments. Generated and vendored files are excluded by default. Empty or omitted `subpath` means repo-root scope. Do not use before graph resolution for symbol questions; use as companion after graph tools surface relevant context. For symbol/relationship questions use query_graph instead. |
| `search_decisions` | Search Decisions | object; required fields: 1 | `stable-object` | exact structuredContent schema | Search persisted decision memory for prior conclusions, linked evidence, and artifact references. Returns stable object `structuredContent` with `query`, `matches`, `summary`, `truncated`, and `warnings`. |
| `search_files` | Search Files | object; required fields: 1 | `stable-object` | exact structuredContent schema | Discover files by name or path glob. Use as a graph/content companion lookup for non-code assets — docs, config, SQL, Markdown, templates — after graph tools have surfaced structural context. Empty or omitted `subpath` means repo-root scope. Do not use before graph resolution for symbol questions. For symbol/relationship questions use query_graph instead. |
| `search_saved_context` | Search Saved Context | object; required fields: 1 | `stable-object` | exact structuredContent schema | Search previously saved artifacts in the content store using BM25 + trigram fallback. Returns stable object `structuredContent` with `query`, preview `matches`, `summary`, `truncated`, and `warnings` for follow-up retrieval. |
| `search_templates` | Search Templates | object; required fields: 0 | `stable-object` | exact structuredContent schema | Discover template files (HTML, Jinja2, Handlebars, Tera, Mako, Mustache, Twig, Liquid, ERB, HAML, Pug) by extension. Use as a graph/content companion lookup when changed files or graph evidence suggests a dependency on template behavior. Empty or omitted `subpath` means repo-root scope. Narrows by `kind` when you know the template engine. Prefer this over search_files for template-specific discovery. For symbol/relationship questions use query_graph instead. |
| `search_text_assets` | Search Text Assets | object; required fields: 0 | `stable-object` | exact structuredContent schema | Discover SQL, config (TOML/YAML/INI), environment (.env), and prompt files. Use as a graph/content companion lookup when changed files include SQL, config, or prompt assets, or when graph evidence suggests a non-code dependency. Empty or omitted `subpath` means repo-root scope. Use `kind` to narrow to a specific asset type. These files are not indexed as graph symbols; use query_graph for symbol/relationship questions. |
| `status` | Status | object; required fields: 0 | `stable-object` | exact structuredContent schema | Return a compact graph health summary: build state, node/edge counts, last-indexed timestamp, and a machine-readable failure category. Call this before query_graph or get_context to verify the graph is healthy and up-to-date. Succeeds even when the graph DB is missing. |
| `symbol_neighbors` | Symbol Neighbors | object; required fields: 1 | `stable-object` | exact structuredContent schema | Return the immediate graph neighbourhood of a symbol: callers, callees, call edge sites with source lines, test nodes, containment siblings, and import-linked nodes. Useful for understanding a symbol's role and exact direct usage sites without a full traversal. |
| `tool_help` | Tool Help | object; required fields: 1 | `stable-object` | exact structuredContent schema | Return runtime manual documentation for one visible exported MCP tool by exact name. Shorthand for man with namespace='mcp'. |
| `tool_list` | Tool List | object; required fields: 0 | `stable-object` | exact structuredContent schema | List visible exported MCP tools in compact runtime inventory form. Use this instead of hardcoding tool tables in agent instructions; pair with tool_search and tool_help for discovery. |
| `tool_search` | Tool Search | object; required fields: 1 | `stable-object` | exact structuredContent schema | Search visible exported MCP tools by name, title, or description without executing them. Ranks matches with explicit lexical score factors and typo-tolerant fuzzy name matching, and returns suggestions when no strong direct match exists. |
| `traverse_graph` | Traverse Graph | object; required fields: 1 | `stable-object` | exact structuredContent schema | Bi-directional graph traversal from a specific symbol (qualified name). Returns all nodes reachable within depth hops. |
| `wake_up` | Wake Up | object; required fields: 0 | `stable-object` | exact structuredContent schema | Bounded session-start recall for hookless agents. Assembles a compact context pack from the resume snapshot, decision memory, saved-context hints, global memory, changed files, and graph readiness, then records the session-start event through the shared event service shared with native hooks. Large saved artifacts are referenced by source_id only, never inlined. |
