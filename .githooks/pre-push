#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[pre-push] test (--workspace)"
./scripts/test-workspace-summary.sh --workspace
echo "[pre-push] test passed"
